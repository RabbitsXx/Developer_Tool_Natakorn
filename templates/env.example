# Add only environment variable NAMES that this project actually requires.
# Keep real values in a local ignored environment file; never commit credentials.
#
# Example only after a capability is selected:
# SERVICE_URL=
# SERVICE_API_KEY=
